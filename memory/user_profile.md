---
name: User profile
description: Developer profile — preferred stack is .NET 10 backend + React frontend, mirrors AppIgreja project
type: user
---

Uses .NET 10 for backend across all projects (AppIgreja reference). Frontend in React + Vite + Tailwind + Radix UI. When starting a new project, always check AppIgreja (/Users/aurelioromeu/repos/AppIgreja) as the reference for patterns — controllers, services, DTOs, apiClient, component structure. User did not specify .NET upfront for this project; Node.js was used initially and later migrated.
